<div class="off-canvas position-left" style="position: fixed; top:0;"id="menu" data-off-canvas>
    <h1>Mersea</h1>

    <!-- Close button -->
    <button class="menu-close-button" aria-label="Close menu" type="button" data-close>
        <span aria-hidden="true"><i class="fi-x"></i></span>
    </button>

    <!-- Menu -->
    <ul class="vertical menu">
        <li>
            <%= link_to_unless_current("Carte", { controller: "pages", action: "index" }) %>
        </li>
        <li>
            <%= link_to_unless_current("Le projet", { controller: "pages", action: "about" }) %>
        </li>
        <li>
            <%= link_to_unless_current("Traceurs", { controller: "tracers", action: "index" }) %>
        </li>
        <li>
            <%= link_to_unless_current("Sécurité", { controller: "pages", action: "security" }) %>
        </li>
        <li>
            <%= link_to_unless_current("ANSEL", { controller: "pages", action: "ansel" }) %>
        </li>
        <li>
            <%= link_to_unless_current("Mentions légales", { controller: "pages", action: "disclaimer" }) %>
        </li>
    </ul>

</div>